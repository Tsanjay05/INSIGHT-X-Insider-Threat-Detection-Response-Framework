server:
  port: 0  # Random port for tests

spring:
  application:
    name: insightx-trust-engine-test

  jackson:
    time-zone: UTC

logging:
  level:
    root: INFO
    com.insightx: DEBUG
    org.testcontainers: INFO
    org.flywaydb: DEBUG
