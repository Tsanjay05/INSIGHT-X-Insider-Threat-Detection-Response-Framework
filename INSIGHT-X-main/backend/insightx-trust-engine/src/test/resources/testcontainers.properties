# Updated Testcontainers configuration for Windows Docker Desktop

docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
ryuk.container.privileged=true
# Docker Desktop on Windows exposes the daemon via the "docker_cli" named pipe.
# Explicitly point Testcontainers to this pipe.

docker.host=tcp://localhost:2375

