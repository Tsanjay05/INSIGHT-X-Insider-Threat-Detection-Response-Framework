# IAM Connector Framework

Connectors for Okta, Azure AD, and other IAM systems.

- Implements configurable connector interface
- Normalizes identity and role data for enrichment
- Supports event-driven sync and batch sync
